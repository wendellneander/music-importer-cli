class Playlist {
  constructor({ id, title, author, artwork, tracks }) {
    this.id = id
    this.title = title
    this.author = author
    this.artwork = artwork
    this.tracks = tracks
  }
}

module.exports = Playlist
